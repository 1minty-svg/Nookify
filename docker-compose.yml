version: '3.8'
services:
  db:
    image: postgres:15
    container_name: nookify-db
    environment:
      - POSTGRES_DB=nookify_db
      - POSTGRES_USER=minty
      - POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"